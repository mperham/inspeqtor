#!/bin/sh

set -e

# https://www.debian.org/doc/debian-policy/ch-maintainerscripts.html

rm -f /etc/init/redacted.conf

if [ "$1" = "purge" ] ; then
  echo "Purging redacted configuration"
  rm -rf /etc/redacted
fi
